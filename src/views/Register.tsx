import React from "react";

import { Container } from "react-bootstrap";
import { Link } from "react-router-dom";

function Register() {
  return (
    <Container>
      Registration is not currently supported. <Link to="/">Go Back</Link>
    </Container>
  );
}

export default Register;
